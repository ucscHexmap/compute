Each installation has its own configuration file which is used to build
the python virtual environment, and to run the server.

These are example configurations

See prod.sh for a commented configuration.
