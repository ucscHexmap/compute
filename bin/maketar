#!/bin/bash
# Build a tarball for the compute server
tar cf compute.tar \
    bin \
    build \
    calc \
    config \
    standalone \
    tests \
    www
scp compute.tar kolossus:/hive/groups/hexmap/dev

