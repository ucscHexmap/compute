#!/bin/bash
# Build a tarball for the compute server
tar czf install.tgz \
    bin \
    build \
    calc \
    config \
    standalone \
    tests \
    www
scp install.tgz hexmap-new:/cluster/home/swat/dev/compute

