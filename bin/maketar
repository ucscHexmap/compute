#!/bin/bash
# Build a tarball for the compute server
tar cf install.tar \
    bin \
    build \
    calc \
    config \
    standalone \
    tests \
    www
scp install.tar kolossus:/cluster/home/swat/dev/compute

