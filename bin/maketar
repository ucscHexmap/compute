#!/bin/bash
# Build a tarball for the compute server
tar cf t.tar \
    bin \
    build \
    calc \
    config \
    standalone \
    tests \
    www
scp t.tar kolossus:/hive/groups/hexmap/dev/compute
rm t.tar
