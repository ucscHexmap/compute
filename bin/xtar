#!/bin/bash
# extract from a tarball of the compute server without docker
mv tests/run testsRun
rm -rf \
    build \
    calc \
    config \
    standalone \
    tests \
    www
tar -xf /hive/groups/hexmap/dev/compute/t.tar
mv testsRun tests/run
