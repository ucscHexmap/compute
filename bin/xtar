#!/bin/bash
# extract from a tarball of the server support files, not www.
mv tests/run testsRun
rm -rf \
    build \
    calc \
    config \
    standalone \
    tests \
    www
tar -xf /hive/groups/hexmap/compute.tar
mv testsRun tests/run
